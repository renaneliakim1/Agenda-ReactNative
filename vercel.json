{
  "buildCommand": "cd meuprimeiroapp && npm install && npx expo export --platform web",
  "outputDirectory": "meuprimeiroapp/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
